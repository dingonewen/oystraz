# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
psycopg2-binary==2.9.9

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # Pin compatible bcrypt version to avoid 72-byte password error
bcrypt==4.0.1  # Pin compatible bcrypt version to avoid 72-byte password error

# Configuration
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0  # Required for EmailStr in pydantic
python-dotenv==1.0.0

# HTTP client
httpx==0.25.1

# Google Gemini AI
google-generativeai==0.8.3

# Development (not needed in production)
# pytest==7.4.3
# pytest-asyncio==0.21.1